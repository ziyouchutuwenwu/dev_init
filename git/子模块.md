# git 子模块用法

## 单独修改了 git 子模块的文件，引用他的仓库需要执行以下命令

```bash
git submodule foreach --recursive git pull origin master
```

## 在引用仓库内，更新子模块

```bash
cd demo_module
git add *
git push -a -m "xxx"
git push
```

## 引用仓库内，子模块的更新和 push 是和主仓库没有关系的，完全独立，因此，需要分开运行
