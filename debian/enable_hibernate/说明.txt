pkaction --version
------------------------------------------------------------------------
高于0.106，
mkdir -p /etc/polkit-1/rules.d
cp ./85-hibernate.rules /etc/polkit-1/rules.d/
------------------------------------------------------------------------
低于0.106
cp ./20-allow-hibernate.pkla /etc/polkit-1/localauthority/50-local.d/
------------------------------------------------------------------------
xfce电源管理器
  一般，笔记本盖，建议设置锁屏或者关机，别的不要设置，否则会黑屏
  系统，点亮不足，同上
  系统，系统节电，可以设置为休眠或者挂起