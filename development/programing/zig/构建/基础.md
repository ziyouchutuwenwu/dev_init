# 基础

## 说明

一些基础函数的说明

```sh
addExecutable   为生成可执行文件
addModule       为生成一个公开可以给别的模块调用的 module, 支持 import 其它module
createModule    创建一个内部 module, 支持 import 其它module
```

```sh
addIncludePath  指定头文件的搜索位置
installHeadersDirectory 将指定目录下的所有头文件复制到目标目录。用于发布库的头文件，供其他项目使用
installHeader   复制单个头文件
```
