# 重定向

## 说明

router 里面不能用
controller, plug 里面可以用

## 用法

```elixir
def index(conn, _params) do
  redirect(conn, external: "https://baidu.com/")
end
```

或者

```elixir
def index(conn, _params) do
  redirect(conn, to: ~p"/aaa")
end
```
