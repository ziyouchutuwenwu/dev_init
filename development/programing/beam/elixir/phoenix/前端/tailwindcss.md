# tailwindcss

## 说明

tailwindcss 手动下载没用

## 例子

```sh
npm install -g esbuild
```

config/config.exs

```elixir
config :esbuild,
  version_check: false,
  path: System.find_executable("esbuild"),
```
