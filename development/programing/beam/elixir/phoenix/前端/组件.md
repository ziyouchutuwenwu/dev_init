# 组件

## 例子

### 创建

```sh
mix phx.new web_demo --no-gettext --no-dashboard --no-live --no-mailer --no-ecto
```

### 路由

```elixir
scope "/", WebDemoWeb do
  pipe_through :browser

  get "/", PageController, :home
  get "/aaa", PageController, :aaa
  get "/bbb", PageController, :bbb
end
```

### controller

```elixir
defmodule WebDemoWeb.PageController do
  use WebDemoWeb, :controller

  def home(conn, _params) do
    render(conn, :home)
  end

  def aaa(conn, _params) do
    render(conn, "aaa.html")
  end

  def bbb(conn, _params) do
    render(conn, :bbb)
  end
end
```

### 页面组件

demo1.ex

```elixir
defmodule WebDemoWeb.Component.Demo1 do
  use Phoenix.Component

  def aaa(assigns) do
    ~H"""
    <div>demo1 aaa 组件，参数 <%= @abc %></div>
    """
  end

  def bbb(assigns) do
    ~H"""
    <div>demo1 bbb 组件，参数 <%= @def %></div>
    """
  end
end
```

demo2/demo2.ex

```elixir
defmodule WebDemoWeb.Component.Demo2 do
  use Phoenix.Component

  embed_templates "*"
end
```

demo2/ccc.html.heex

```html
<div>demo2 ccc 组件，参数 <%= @abc %></div>
```

demo2/ddd.html.heex

```html
<div>demo2 ddd 组件，参数 <%= @def %></div>
```

### heex

aaa.html.heex

```html
<div>aaa 开始</div>
<WebDemoWeb.Component.Demo1.aaa abc="参数 abc" />
<WebDemoWeb.Component.Demo1.bbb def="参数 def" />
<div>aaa 结束</div>
```

bbb.html.heex

```html
<div>bbb 开始</div>
<WebDemoWeb.Component.Demo2.ccc abc="参数 abc" />
<WebDemoWeb.Component.Demo2.ddd def="参数 def" />
<div>bbb 结束</div>
```
