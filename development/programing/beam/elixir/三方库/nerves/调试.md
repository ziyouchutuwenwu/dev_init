# 调试

## 方法

显示帮助

```elixir
h(Toolshed)
cmd("busybox")
```

banner

```elixir
NervesMOTD.print()
```

log

```elixir
# 手动看
RingLogger.next()

# 查看最新 10 条
RingLogger.tail(10)
```

rootfs_overlay/etc/iex.exs

```sh
# 这里加上，就能在串口看到
RingLogger.attach()
```
