# 特定硬件

## 例子

config/config.exs

```elixir
if Mix.target() != :host do
  import_config "#{Mix.target()}.exs"
end
```

bbb.exs

```elixir
# 单独配置，编译的时候会加载这里的配置
```

编译

```elixir
MIX_TARGET=bbb mix firmware
```
