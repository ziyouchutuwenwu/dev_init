# 通用

## 说明

start_link 返回的是父进程 pid

transient 和 permanent 模式下，子进程跟着父进程自动启动

## 代码

demo_sup1.ex

```elixir
defmodule DemoSup1 do
  use Supervisor

  def start_link(init_arg1, init_arg2) do
    Supervisor.start_link(__MODULE__, [init_arg1, init_arg2], name: __MODULE__)
  end

  def init([init_arg1, init_arg2]) do
    children = [
      %{
        id: DemoServer,
        start: {DemoServer, :start_link, [init_arg1, init_arg2]},
        restart: :permanent
        # restart: :transient
        # restart: :temporary
        # type: :supervisor
      }
    ]

    Supervisor.init(children, strategy: :one_for_one)
  end
end
```

demo_sup2.ex

```elixir
defmodule DemoSup2 do
  use Supervisor

  def start_link() do
    Supervisor.start_link(__MODULE__, [], name: __MODULE__)
  end

  def init([]) do
    Supervisor.init([], strategy: :one_for_one)
  end

  def start_child(init_arg1, init_arg2) do
    child_spec = %{
      id: DemoServer,
      start: {DemoServer, :start_link, [init_arg1, init_arg2]},
      # 手动启动，不能用 permanent
      restart: :transient,
      type: :worker
    }

    Supervisor.start_child(__MODULE__, child_spec)
  end
end
```

demo_server.ex

```elixir
defmodule DemoServer do
  use GenServer
  require Logger

  def start_link(init_arg1, init_arg2) do
    {:ok, pid} = GenServer.start_link(__MODULE__, {init_arg1, init_arg2}, name: __MODULE__)
    Logger.debug("gen_server pid is #{inspect(pid)}")
    {:ok, pid}
  end

  def queue do
    GenServer.call(__MODULE__, :queue)
  end

  def dequeue do
    GenServer.call(__MODULE__, :dequeue)
  end

  def stop() do
    GenServer.stop(__MODULE__)
  end

  def async_stop do
    GenServer.cast(__MODULE__, :stop)
  end

  # -----------------------------------------------------------------
  def init(state) do
    {:ok, state}
  end

  def handle_call(:dequeue, _from, [value | state]) do
    {:reply, value, state}
  end

  def handle_call(:dequeue, _from, []) do
    {:reply, nil, []}
  end

  def handle_call(:queue, _from, state) do
    {:reply, state, state}
  end

  def handle_call(:stop, _from, state) do
    {:stop, :normal, "server stopped", state}
  end

  def handle_cast(:stop, state) do
    {:stop, :normal, state}
  end

  def terminate(reason, _state) do
    IO.inspect("reason #{inspect(reason)} in terminate")
  end
end
```

或者

```elixir
defmodule DemoServer do
  require Logger

  def start_link(init_arg1, init_arg2) do
    pid = spawn_link(__MODULE__, :loop, [init_arg1, init_arg2])
    {:ok, pid}
  end

  def loop(init_arg1, init_arg2) do
    Logger.debug("#{inspect(__MODULE__)} loop #{init_arg1} #{init_arg2}")

    receive do
      msg ->
        Logger.debug("received msg #{inspect(msg)} #{init_arg1} #{init_arg2}")
        loop(init_arg1, init_arg2)
    end
  end
end
```

## 测试

demo.ex

```elixir
defmodule Demo do
  require Logger

  def demo1 do
    DemoSup1.start_link(111, 222)
    [{_id, pid, _, _}] = Supervisor.which_children(DemoSup1)
    Process.exit(pid, :shutdown)

    Process.sleep(1000)

    case Supervisor.which_children(DemoSup1) do
      [] ->
        Logger.debug("worker not exist")

      [{_id, :undefined, _type, _modules}] ->
        Logger.debug("worker not alive")

      [{_id, pid, _type, _modules}] ->
        send(pid, {"1111111111111"})
    end

    Supervisor.terminate_child(DemoSup1, DemoServer)
  end

  def demo2 do
    DemoSup2.start_link()
    DemoSup2.start_child(333, 444)
    Process.sleep(1000)

    # Supervisor.terminate_child(DemoSup2, DemoServer)
    Supervisor.which_children(DemoSup2)
  end
end
```
