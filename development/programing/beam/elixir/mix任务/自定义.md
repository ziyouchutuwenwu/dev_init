# 自定义

## 例子

```elixir
# 必须以 Mix.Tasks 开头
defmodule Mix.Tasks.Demo.Task1 do
  use Mix.Task

  @shortdoc "描述必须有，否则找不到任务"

  def run(args) do
    Mix.shell().info("测试任务 #{args}")
  end
end
```

编译

```sh
mix compile
mix help --search task
mix demo.task1 111
```
