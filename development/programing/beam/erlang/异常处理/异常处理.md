# 异常处理

## 例子

```erlang
-module(demo).
-export([demo/2]).

demo(A, B) ->
    try A / B of
        Result -> {ok, Result}
    catch
        error:badarith -> {error, division_by_zero};
        error:Reason:Stack -> {error, Reason, Stack}
    end.
```
