{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "debug",
      "type": "gdb",
      "request": "launch",
      "target": "${workspaceFolder}/target/debug/${workspaceFolderBasename}",
      "cwd": "${workspaceRoot}"
    }
  ]
}
