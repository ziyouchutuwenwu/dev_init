# 其它语言

## 说明

其它语言调用这个动态库

## 步骤

创建项目

```sh
cargo new demo_c_so --lib
```

Cargo.toml

```toml
[lib]
crate-type = ["cdylib"]
```

lib.rs

```rust
// 给其他语言调用
// 不启用修饰，修饰完以后，名字会变化，其它语言就找不到这个导出符号
#[unsafe(no_mangle)]
pub extern "C" fn add(left: u64, right: u64) -> u64 {
    left + right
}
```
