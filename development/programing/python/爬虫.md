# 爬虫

## 说明

playwright，调用浏览器，基本上可以通杀 js

## 配置

mirror

```sh
export PLAYWRIGHT_DOWNLOAD_HOST="https://npmmirror.com/mirrors/playwright"
```

```sh
# 完整版
playwright install chromium

# 无头版，更快更小
playwright install chromium-headless-shell
```
