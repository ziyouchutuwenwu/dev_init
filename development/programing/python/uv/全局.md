# 全局

## 用法

### 创建

```sh
uv venv dev_env --python 3.12
source ./dev_env/bin/activate
```

### 依赖

```sh
# source 后执行，会安装到激活的 venv 的目录里面
uv pip install xxx
```

```sh
# 当前 venv 里面的包写入 requirements.txt
uv pip freeze > requirements.txt
```

同步依赖

```sh
uv pip sync ./requirements.txt
```
