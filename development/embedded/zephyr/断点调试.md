# 断点调试

## 准备工作

openocd

[用这个](https://github.com/espressif/openocd-esp32/releases)

启用 jtag

```sh
espefuse -p /dev/ttyACM0 burn-efuse STRAP_JTAG_SEL
```

测试

```sh
# esp_usb_bridge.cfg 里面有 lsusb 的vendor_id 和 product_id，必须对应
openocd -f interface/esp_usb_bridge.cfg -f target/esp32s3.cfg
```
