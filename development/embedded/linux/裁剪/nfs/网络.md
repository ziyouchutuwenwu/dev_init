# 网络

## 说明

想要实现 qemu 虚拟机和外部通信，只能使用 tap/bridge 模式

## 步骤

### 安装工具

```sh
sudo apt install uml-utilities bridge-utils
```

### 配置网卡

用的是 virt-manager 自带的 nat 网络

```sh
sudo sh -c 'echo "allow virbr0" >> /etc/qemu/bridge.conf'
```

### 运行

```sh
sudo qemu-system-arm \
  -M vexpress-a9 \
  -m 512M \
  -kernel ~/downloads/linux-5.10.191/output/arch/arm/boot/zImage \
  -dtb ~/downloads/linux-5.10.191/output/arch/arm/boot/dts/vexpress-v2p-ca9.dtb \
  -nographic \
  -append "root=/dev/mmcblk0 rw console=ttyAMA0" \
  -drive file=$HOME/downloads/rootfs.ext4,if=sd,format=raw \
  -netdev bridge,id=net0,br=virbr0 \
  -net nic,netdev=net0
```

在 qemu 的虚拟机里面

/etc/init.d/rcS

```sh
ifconfig eth0 10.0.2.211 netmask 255.255.255.0 up
route add default gw 10.0.2.1
echo "nameserver 10.0.2.1" > /etc/resolv.conf
```
