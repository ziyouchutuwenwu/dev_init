# 训练

## 说明

自己训练

## 步骤

数据标注

```sh
# 找支持自动标注的平台
自动标注
    一种自动帮你选择图片里面全部一样的
    一种是全部傻瓜式，点击一下全部自动标注好的
```

训练

yaml 是物体类型，标注自动导出的

```python
from ultralytics import YOLO

model = YOLO("yolov11n.pt")
model.train(
    data="../coco128/coco128.yaml",
    epochs=10,
    imgsz=640,
    device="0,1",
    amp=False,
    # 保存路径
    project="/xxx/xxxx",
    save=True
)
```
