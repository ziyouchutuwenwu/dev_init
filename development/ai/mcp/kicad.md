# kicad

## 说明

kicad 的 mcp

## 分类

### 准备

```sh
kicad -> 设置 -> 偏好设置 -> 插件 -> 启用 KiCad API
```

### kicad-mcp-pro

```sh
uv pip install kicad-mcp-pro
```

启动

```sh
kicad-mcp-pro
```

环境变量

```sh
"KICAD_MCP_PROJECT_DIR": "项目绝对路径"
"KICAD_MCP_OPERATING_MODE": "write"
```
