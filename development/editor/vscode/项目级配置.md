# 项目级配置

## 说明

有些配置不希望配置在全局，可以在每个项目下单独配置

## 方式

项目目录下

.vscode/settings.json

```json
{
  "python.defaultInterpreterPath": "${env:VIRTUAL_ENV}",
  // 设置比较大的深度，防止快速修复失败
  "python.analysis.packageIndexDepths": [{ "name": "", "depth": 10 }]
}
```
