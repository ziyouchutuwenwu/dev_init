# 虚拟摄像头

## 步骤

创建

```sh
sudo modprobe v4l2loopback video_nr=10 exclusive_caps=1
```

推视频

```sh
ffmpeg -re -stream_loop -1 -i ~/downloads/video/001.avi -f v4l2 /dev/video10
```

测试

```sh
ffplay /dev/video10
```
