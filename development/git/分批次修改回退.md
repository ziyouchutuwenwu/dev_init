# 分批次修改回退

## 用法

```sh
# 第一批更改
git add .
git commit -m "批量更新 - 2025-12-29 14:30"
git tag -a v1 -m "第一批批量更新"

# 第二批更改
git add .
git commit -m "批量更新 - 2025-12-29 15:00"
git tag -a v2 -m "第二批批量更新"

# 查看
git tag

# 回退到第一批
git checkout v1

# 回到最新版本
git checkout main
```
