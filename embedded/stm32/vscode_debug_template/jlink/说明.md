# vscode 使用 jlink 调试 stm32, 如果 jlink 是挂到openocd上调试，不需要配置这个

- 全局settings.json里面, "cortex-debug.JLinkGDBServerPath"必须要有，例如

```shell
"cortex-debug.JLinkGDBServerPath": "/home/mmc/dev/embedded/stm32/JLink/JLinkGDBServerCLExe"
```