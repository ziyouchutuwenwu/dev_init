# vscode 使用 jlink 调试 stm32, 如果 jlink 是挂到 openocd 上调试，不需要配置这个

- 全局 settings.json 里面, "cortex-debug.JLinkGDBServerPath"必须要有，例如

```shell
"cortex-debug.JLinkGDBServerPath": "/home/xxx/dev/embedded/stm32/JLink/JLinkGDBServerCLExe"
```
