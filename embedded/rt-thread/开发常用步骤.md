# 开发步骤

## Kconfig 配置

- 需要对照别的 bsp 在 Kconfig 里面添加 i2c 或者 spi 等配置菜单
- 具体的引脚，在 cubeMX 里面看到以后，需要对着 libraries/HAL_Drivers/drv_gpio.c 里面找\_\_STM32_PIN 宏所对应的引脚

## 启用

- scons --menuconfig 启用相应的配置，设置相关的引脚号

## 编译

- scons

## 测试

- 进入 shell，list_device 查看 i2c 是否启用，当然需要同时启用串口了
